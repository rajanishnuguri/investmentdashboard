{
  "name": "wealth-trajectory",
  "version": "1.0.0",
  "description": "Independent broker dashboard that talks to MCP servers directly via its own backend (no Anthropic in the middle).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@babel/core": "^8.0.1",
    "@babel/preset-react": "^8.0.1",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
